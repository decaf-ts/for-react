/**
 * Placeholder for the Angular DynamicModule.
 * React does not need runtime module registration, but we export
 * a lightweight helper to maintain API parity.
 */
export class DynamicModule {
  static register() {
    return {};
  }
}
